const mySelf={
    'name':'kirtan',
    'age':22,
    'degree':'bTech',
    'collegeName':'BVM'
}

mySelf.address='anand';
// console.log(mySelf);
// objlength=Object.keys(mySelf).length;
// console.log(objlength);
objkey=Object.keys(mySelf);
console.log(`this is a key of object ${objkey}`);
objval=Object.values(mySelf);
console.log(`this is the value of object ${objval}`);

